import { test } from '@japa/runner'

test.group('User/count', () => {
  test('Succesfully counts', async ({
    /* assert */
  }) => {})
  test('Fails to count', async ({
    /* assert */
  }) => {})
  test('Succesfully counts', async ({
    /* assert */
  }) => {})
})
